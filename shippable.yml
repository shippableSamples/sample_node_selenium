language: node_js

node_js:
  - 0.10

services:
  - selenium

addons:
  chrome: "49.0"

build:
  pre_ci_boot:
    image_name: drydock/u14pls
    image_tag: prod
    pull: true

  ci:
    - sudo npm install
    - export DISPLAY=:99.0
    - xvfb-run --server-args="-ac" npm test
