build_image: drydock/u14pls:prod

language: node_js

node_js:
  - "0.10"

services:
  - selenium

addons:
  chrome: "49.0"

script:
  - sudo npm install
  - export DISPLAY=:99.0
  - xvfb-run --server-args="-ac" npm test
